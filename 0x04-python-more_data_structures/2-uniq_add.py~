#!/usr/bin/python3
# add all unique elements in a list

def uniq_add(my_list=[]):
    # I'll use set instaed of making a new list
    check = set(my_list)
    sum = 0
    for i in my_list:
        if i in check:
            continue
        sum += i
    return (sum)
