#!/usr/bin/python3
# update te value of a key in a dict

def update_dictionary(a_dictionary, key, value):
    # create one if it doesn't exists
    a_dictionary[key] = value
    return (a_dictionary)
